import DashboardProductsTable from "../layouts/tables"; // import with a different name

export default function DashboardProductsPage() {
    return <DashboardProductsTable />; // now it correctly uses the imported component
}

